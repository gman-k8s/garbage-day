{% if events %}Diese Woche: {% for e in events %}{{ e.summary }} am {{ e.date|weekday }}, {{ e.date.day }}.{{ e.date.month }}.{{ e.date.year }}{% if not loop.last %} · {% endif %}{% endfor %}{% elif next_event %}Nächster Termin: {{ next_event.summary }} am {{ next_event.date|weekday }}, {{ next_event.date.day }}.{{ next_event.date.month }}.{{ next_event.date.year }}{% endif %}
